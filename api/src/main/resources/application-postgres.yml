spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5433/workpoints}
    username: ${DB_USERNAME:workpoints}
    password: ${DB_PASSWORD:workpoints}
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  h2:
    console:
      enabled: false
